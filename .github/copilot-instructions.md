# Copilot Instructions

## Project Guidelines
- When a build fails due to a locked process (e.g., apphost.exe), ask the user to kill it via Task Manager rather than trying to run Stop-Process commands in the terminal.